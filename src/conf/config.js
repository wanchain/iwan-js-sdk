const config = {
    socketUrl: 'api.wanchain.org',
    socketPort: 443,

    apiFlag: 'ws',
    apiVersion: 'v3',

    _Encoding: {
        _enc: 'sha256',
        _base64: "base64",
        _hex: "hex"
    }
}

module.exports = config;